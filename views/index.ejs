<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link rel="stylesheet" href="/styles.css">

  <title>Notes App</title>
</head>
<body>

<h1>Notes App</h1>

<h2>Add a note: </h2>
<form class="create-form" action="/createNote" method="post">
    <input type="text" name="title" placeholder="Enter a title">
    <textarea type="text" rows="5" cols="40" name="content" placeholder="Enter a note"></textarea>
    <button type="submit">Add Note</button>
</form>

<h2>My Notes: </h2>
<ul>
    <% for (let i = 0; i < notes.length; i++) { %>
        <li>
            <h3><%= notes[i].title %></h3>
            <p><%= notes[i].content %></p>
            <!--delete button-->
            <form class="delete-form" action="/deleteNote" method="post">
                <input type="hidden" name="id" value="<%= notes[i].id %>">
                <button type="submit">Delete</button>
            </form>
        </li>
    <% } %>
</ul>

</body>